"use client";

import { useRouter } from "next/navigation";

export default function Page() {
  // typeof window !== "undefined" ? window.localStorage.setItem("access", "dsadsdasdasdasd") : false;

  //azinjas bara authentication agar nadasht
  // const router = useRouter();
  // if (typeof window !== "undefined") router.push("/login");

  return <></>;
}
